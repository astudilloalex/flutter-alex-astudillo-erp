import 'package:flutter/material.dart';

class HomeCompanyDialog extends StatelessWidget {
  const HomeCompanyDialog({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Dialog();
  }
}
